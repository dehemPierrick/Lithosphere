{% extends 'base.html.twig' %}

{% block title %}{{ product.name}}  - Lithosphère France{% endblock %}

{% block content %}
<section class="container">
    <div class="row">
        <div class="col-md-5">
            <img src="/uploads/{{product.illustration}}" alt="{{product.name}}" class="img-fluid"></a>
        </div>
        <div class="col-md-7 my-auto">
            <h3>{{ product.name}} :</h3>
            <p>{{ product.description}}</p>
            <span class="" >Prix : {{(product.price /100) |number_format(2, ',','.') }} €</span>
            <hr>
            <span class="" >Frais de port : {{(product.transportPrice /100) |number_format(2, ',','.') }} €</span>
            <hr>
            <p>
                {{product.motsClef}}
            </p>
            <hr>
            <p>
               {{product.descriptionVertu1|upper}} :
            </p>
            <p>
                {{product.vertus1|nl2br}}
            </p>
            <hr>
            <p>
                {{product.descriptionVertu2|upper}} :
            </p>
            <p>
                {{product.vertus2|nl2br}}
            </p>
            
            <a href="#" class="mb-4 btn btn-primary">Ajouter au panier</a>
        </div>
    
    
    </div><!-- /.row -->
</section>

    
{% endblock %}
