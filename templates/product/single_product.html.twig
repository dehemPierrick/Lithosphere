<div class="text-center my-4">
    <a href="{{ path('product', { 'slug' : product.slug }) }}"><img src="/uploads/{{product.illustration}}" alt="{{product.name}}" class="img-fluid"></a>
    <h5 class="mt-4">{{product.name}}</h5>
    <p class="mt-2" >{{product.description}}</p>
    <span class="mt-4" >{{(product.price /100) |number_format(2, ',','.') }} €</span>
    <a href="{{ path('product', { 'slug' : product.slug }) }}"  class="read-more-btn2 btn">Voir le produit</a>
</div>